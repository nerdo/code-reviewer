{
  "watch": ["src/server", "src/domain", "src/infrastructure"],
  "ext": "ts",
  "exec": "ts-node --project tsconfig.server.json --require tsconfig-paths/register src/server/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}